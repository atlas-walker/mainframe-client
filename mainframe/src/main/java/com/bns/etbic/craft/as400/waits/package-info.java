/**
 * Wait conditions evaluated against screen snapshots while the driver waits for the
 * host. {@code As400Conditions} provides the built-in conditions; {@code
 * ExpectedCondition} is the extension point.
 *
 * @author Andres Acosta
 * @since 0.1.0
 */
package com.bns.etbic.craft.as400.waits;
